<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.truckpadcase.calculatefreight">

    <!-- PERMISSIONS  -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_truck_laucher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- ACTIVITYS   -->
        <activity
            android:name=".presentation.views.SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".presentation.views.CalculateFreightActivity"
            android:label="@string/title_activity_maps" />

        <activity
            android:name=".presentation.views.HistorySearchActivity"
            android:parentActivityName=".presentation.views.CalculateFreightActivity"
            android:theme="@style/AppTheme2"/>

        <activity
            android:name=".presentation.views.ResultSearchActivity"
            android:parentActivityName=".presentation.views.CalculateFreightActivity"
            android:theme="@style/AppTheme2" />

        <activity
            android:name=".presentation.views.RequestActivity"
            android:parentActivityName=".presentation.views.CalculateFreightActivity"
            android:theme="@style/AppTheme2" />
        <!--        -->

    </application>

</manifest>